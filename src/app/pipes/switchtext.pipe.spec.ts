import { SwitchtextPipe } from './switchtext.pipe';

describe('SwitchtextPipe', () => {
  it('create an instance', () => {
    const pipe = new SwitchtextPipe();
    expect(pipe).toBeTruthy();
  });
});
